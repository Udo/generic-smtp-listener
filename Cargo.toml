[package]
name = "smtp-receiver"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.37", features = ["fs", "io-util", "macros", "net", "rt-multi-thread", "signal", "sync", "time"] }

[dev-dependencies]
tempfile = "3.10"
